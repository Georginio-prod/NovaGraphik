# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & builds
node_modules
**/node_modules
dist
**/dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Nuxt UI auto-generated (inside app/)
app/auto-imports.d.ts
app/components.d.ts

# Design system handoff (reference only, not committed)
.design-ref

# Local env (root + per-workspace)
.env
.env.local
.env.*.local
app/.env
app/.env.local
backend/.env
backend/.env.local

# SQLite database (created at runtime)
backend/*.sqlite
backend/*.sqlite-*

# Uploaded media (created at runtime — only seed reference images are kept)
backend/uploads/*
!backend/uploads/wp-*
!backend/uploads/samuel-*
!backend/uploads/web-*
# Portfolio source images scraped from novagraphik.fr (kept for reproducibility)
!backend/uploads/flyer-*
!backend/uploads/logo-*
!backend/uploads/motion-*
!backend/uploads/video-*

# Supabase CLI temp
supabase/.temp/
